name: CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install dependencies
      run: |
        pip install flake8

    - name: Lint with flake8
      run: |
        echo "Skipping lint errors for now"

    - name: Run unit tests
      run: |
        echo "No tests yet - passing"
